# uv environment setup

- Date: 2026-09-10; uncommitted.
- Plan: [uv environment setup](../implementation_plan/2026-09-10-uv-setup.md).

## Changes

- Added `scripts/setup_uv.py` and `requirements.uv.txt` for Conda-free Python
  3.11 setup into `.venv`. Existing Conda setup remains available.
- Installs the project editably with notebook/dev extras and reference numerical
  versions, vendored Kilosort runtime dependencies, commit-pinned Phy, Git nelpy,
  and neuro-analysis-py. Does not copy unrelated packages or duplicate CUDA
  runtime pins from the full Conda environment export.
- Uses uv's PyTorch backend support with torch 2.9.1/torchvision 0.24.1 and default
  cu130; explicit cpu/cu126/cu128 choices are supported. Reinstalls those two
  packages on rerun so an explicit backend change takes effect.
- Targets the venv Python explicitly for installation and checks even under
  active Conda. Refuses malformed existing environments and Python other than 3.11.
- Runs `uv pip check` and verifies Phy/nelpy summary imports and vendored Kilosort
  source. Updated README with activation and external prerequisite instructions.
- Source: https://docs.astral.sh/uv/guides/integration/pytorch/ for `uv pip`
  backend selection. No scientific algorithm or parameter changes.

## Verification

- `/local/workdir/ys2375/miniforge3/envs/spn/bin/python -m pytest tests/test_setup_env.py -q`
  — 14 passed. Covers Linux/Windows paths, fresh/reused venv command targeting,
  active Conda isolation, missing uv, refusing non-environment directories,
  dependency coverage and source declarations, plus existing Conda regressions.
- `python scripts/setup_uv.py --help` — passed.
- `python scripts/setup_uv.py` — exited before mutation with the expected
  `uv was not found` message. uv is not installed in this execution environment.
- `git diff --check` — passed; inspected final task diff.

## Limitations

No actual uv solve/install, GPU execution, or Windows installation was performed.
Network DNS failure was already observed in this session. This is a uv pip
workflow, not a validated uv.lock; transitive versions remain solver-selected,
and nelpy uses the unpinned URL required by neuro-analysis-py. System GUI libraries,
Git, GPU drivers, MATLAB, and external Phy plugins are host prerequisites.
